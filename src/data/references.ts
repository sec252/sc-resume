export interface Reference {
  name: string;
  title: string;
  quote: string;
  linkedinUrl?: string;
}

// Add recommendations here once copied over from LinkedIn — the References
// page shows an empty state pointing to your LinkedIn profile until then.
export const references: Reference[] = [];
